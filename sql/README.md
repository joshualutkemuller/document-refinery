# SQL

The canonical deployable DDL is packaged at
`src/document_refinery/sql/001_foundation.sql` so it ships with the Python
distribution. Platform migrations should execute that resource and record the
artifact hash and deployment identity.

